{
  "framework": "nextjs",
  "installCommand": "cd ../.. && pnpm install",
  "buildCommand": "cd ../.. && DATABASE_URL=\"${DIRECT_DATABASE_URL:-$DATABASE_URL}\" pnpm --filter @hotel-pricing/db exec prisma migrate deploy && pnpm --filter @hotel-pricing/db exec prisma generate && pnpm --filter @hotel-pricing/web build"
}
