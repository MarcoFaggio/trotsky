/** @type {import('tailwindcss').Config} */
/**
 * Tailwind v4 is configured via CSS (`globals.css` + `@tailwindcss/postcss`).
 * Kept for tooling that still looks for a JS config.
 */
module.exports = {
  content: ["./src/**/*.{ts,tsx}"],
};
